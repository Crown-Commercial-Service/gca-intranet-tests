# Tell the test helpers to use docker + where your docker compose repo is
WP_DRIVER=docker
WP_DOCKER_CWD=
WP_ADMIN_USERNAME=adminuser
# Must match what you used when you started the stack
WP_ENV_FILE=.env.qa

# Optional but safe
WP_SERVICE=wordpress
PARALLEL_LOCAL=false